SET QUOTED_IDENTIFIER ON
GO
SET ANSI_NULLS ON
GO
CREATE PROCEDURE [dbo].[spGetTCMedicalbyPK]

(@TCMedicalPK int)
AS
SET NOCOUNT ON;

SELECT * 
FROM TCMedical
WHERE TCMedicalPK = @TCMedicalPK
GO
