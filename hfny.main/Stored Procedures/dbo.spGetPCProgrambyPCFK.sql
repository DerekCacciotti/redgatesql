SET QUOTED_IDENTIFIER ON
GO
SET ANSI_NULLS ON
GO

CREATE PROCEDURE [dbo].[spGetPCProgrambyPCFK]
	@PCFK [int]
AS

SET NOCOUNT ON;

SELECT * 
FROM PCProgram WITH (NOLOCK)
WHERE PCFK = @PCFK 
GO
