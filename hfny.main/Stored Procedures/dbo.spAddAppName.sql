SET QUOTED_IDENTIFIER ON
GO
SET ANSI_NULLS ON
GO
CREATE PROCEDURE [dbo].[spAddAppName](@Name varchar(50)=NULL)
AS
INSERT INTO AppName(
Name
)
VALUES(
@Name
)

SELECT SCOPE_IDENTITY() AS [SCOPE_IDENTITY]
GO
